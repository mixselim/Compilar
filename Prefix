#include<stdio.h>
#include<string.h>
#include<ctype.h>
int main()
{
    char a[99999];
    int i,j,len;
    scanf("%s",a);
    len = strlen(a);
    for(i=0;i<len;i++)
    {
        for(j=0;j<=i;j++)
        {
            printf("%c",a[j]);
        }
        printf("\n");
    }
    return 0;
}
